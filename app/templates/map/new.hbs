<h3 class="ui header">{{t "forms.map.caption"}}</h3>
<form class="ui {{state}} form flexberry-vertical-form" role="form">
  {{ui-message
    type="success"
    closeable=true
    visible=showFormSuccessMessage
    caption=formSuccessMessageCaption
    message=formSuccessMessage
    onShow=(action "onSuccessMessageShow")
    onHide=(action "onSuccessMessageHide")
  }}
  {{ui-message
    type="error"
    closeable=true
    visible=showFormErrorMessage
    caption=formErrorMessageCaption
    message=formErrorMessage
    onShow=(action "onErrorMessageShow")
    onHide=(action "onErrorMessageHide")
  }}
  <div class="field">
    <div class="sixteen wide">
      {{flexberry-validationsummary errors=model.errors}}
    </div>
  </div>
  <div class="field">
    <div class="flexberry-edit-panel">
      {{#unless readonly}}
        {{#unless (and hasParentRoute (not saveBeforeRouteLeave))}}
          <button type="submit" class="ui positive button" {{action "save"}}>{{t "forms.edit-form.save-button-text"}}</button>
        {{/unless}}
        {{#unless (and hasParentRoute (not saveBeforeRouteLeave))}}
          <button type="submit" class="ui positive button" {{action "saveAndClose"}}>{{t "forms.edit-form.saveAndClose-button-text"}}</button>
        {{/unless}}
        {{#unless (and model.isNew (or (not hasParentRoute) (and hasParentRoute saveBeforeRouteLeave)))}}
          <button type="submit" class="ui negative button" {{action "delete"}}>{{t "forms.edit-form.delete-button-text"}}</button>
        {{/unless}}
      {{/unless}}
      <button type="submit" class="ui button" {{action 'close'}}>{{t "forms.edit-form.close-button-text"}}</button>
    </div>
  </div>
  <div class="field {{if model.errors.name "error" ""}}">
    {{flexberry-field
      value=model.name
      label=(t "forms.map.name.caption")
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.name pointing="pointing"}}
  </div>
  <div class="field {{if model.errors.lat "error" ""}}">
    {{flexberry-field
      value=model.lat
      label=(t "forms.map.lat.caption")
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.lat pointing="pointing"}}
  </div>
  <div class="field {{if model.errors.lng "error" ""}}">
    {{flexberry-field
      value=model.name
      label=(t "forms.map.lng.caption")
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.lng pointing="pointing"}}
  </div>
  <div class="field {{if model.errors.zoom "error" ""}}">
    {{flexberry-field
      value=model.zoom
      label=(t "forms.map.zoom.caption")
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.zoom pointing="pointing"}}
  </div>
  <div class="field {{if model.errors.coordinateReferenceSystem "error" ""}}">
    {{flexberry-field
      value=model.coordinateReferenceSystem
      label=(t "forms.map.coordinateReferenceSystem.caption")
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.coordinateReferenceSystem pointing="pointing"}}
  </div>
  <div class="field {{if model.errors.public "error" ""}}">
    <label>
      {{t "forms.map.public.caption"}}
    </label>
    {{flexberry-checkbox
      value=model.public
      readonly=readonly
    }}
    {{flexberry-validationmessage error=model.errors.public pointing="pointing"}}
  </div>
</form>
